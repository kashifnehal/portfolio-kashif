# Development Notes

## Commands

- `npm run dev`: starts the Next.js development server.
- `npm run lint`: runs `next lint`.
- `npm run build`: creates the production build.
- `npm run start`: starts the production server after a successful build.

## Environment baseline

- Node: `v23.11.0`.
- npm: `10.9.2`.
- Yarn: `4.6.0`.
- Declared package manager: npm `10.9.2`.
- Lockfiles: `package-lock.json`; the stale Yarn lockfile has been removed.

The canonical package manager and supported Node version are `Not yet verified`.

## Current conventions

TypeScript strict mode is enabled. Next.js compiler support for styled-components is enabled in `next.config.mjs`. No formatter configuration was found.
