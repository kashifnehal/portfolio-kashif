# Design Notes

The intended direction is a personal portfolio with a minimal, typography-led, brutalist visual language inspired by the supplied reference while using the portfolio owner's identity and content.

Required design concerns:

- Responsive layout and predictable project interactions.
- Polished motion, scroll behavior, transitions, and interactive details.
- Semantic HTML and accessibility from the beginning.
- `prefers-reduced-motion` support.
- CSS variables for design tokens.

Current visual audit, final palette, type scale, spacing system, breakpoint behavior, and motion specification: `Not yet verified`.
